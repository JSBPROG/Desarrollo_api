# Variables de entorno para la API de la biblioteca

# Clave secreta para Flask (usada para sesiones, etc.)
# Se recomienda una cadena larga y aleatoria.
SECRET_KEY="tu_clave_secreta_aqui"

# URL de conexión a la base de datos (formato SQLAlchemy)
# Ejemplo para PostgreSQL: DATABASE_URL="postgresql://user:password@host:port/dbname"
# Ejemplo para SQLite (desarrollo): DATABASE_URL="sqlite:///biblioteca.db"
DATABASE_URL="postgresql://postgres:admin@localhost:5432/biblioteca_db"

# Clave secreta para firmar los JSON Web Tokens (JWT)
# Debe ser una cadena segura y única.
JWT_SECRET_KEY="tu_clave_secreta_para_jwt_aqui"
